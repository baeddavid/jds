class Link {
    constructor(object) {
        this.object = object;
        // next pointer default is null
        this.next = null;
        // prev pointer default is null
        this.prev = null;
    }
}

module.exports = Link;